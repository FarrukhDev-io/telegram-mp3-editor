version: '3.8'

services:
  mp3-editor-bot:
    build: .
    container_name: mp3-editor-bot
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - WEB_APP_URL=${WEB_APP_URL}
      - PORT=3000
